# 施工路线图

> 更新时间：2026-07-24

## 当前状态

核心命题已验证：`自然语言 → LLM → M-IR → M-Token 字节码 → 执行`，端到端跑通（qwen3:8b 冒烟 3/3）。

**已通**：
- MVM 虚拟机（80+ 指令，完整安全约束）
- ESP8266 固件（串口执行字节码）
- MCP 工具层（25+ 工具）+ Skills 编排（4 个工作流）
- M-IR 编译器 + PC 端模拟器（15 个测试全绿）
- PC 资源设备化（文件槽，沙箱 + 路径穿越拦截）

**未通**：
- 真实服务链路（tool-router/WebUI 未接 M-IR）
- 控制计算机（仅文件写一个残缺切口）
- eBPF 策略平面（一行未写）
- 打包/安装/服务化（不存在）

---

## 阶段一：接通断点（真实服务链路）

**目标**：用户在 WebUI 说话 → LLM 生成 M-IR → 编译执行 → 返回结果

- [x] `execute_lir_mcp` / `execute_lir_action_mcp` 已在 server.py 中
- [x] 补充 Skills 层测试（15 个用例，4 个工作流全覆盖）

**验证**：39 个测试全绿（24 M-IR + 15 Skills）

---

## 阶段二：PC 资源做实

**目标**：从"沙箱写预设内容"到"动态内容"

- [x] 文件槽：支持动态内容（`source_device` 从传感器/继电器获取值）
- [x] 进程槽：proc0 支持执行命令、返回退出码
- [x] 网络槽：net0 支持 TCP 收发

**验证**：24 个测试全绿

---

## 阶段三：端到端验证（本机 Windows）

**目标**：Ollama 在本机，通过自然语言真正控制 Windows 文件/进程/网络

架构：
```
Ollama（Windows）→ tool_router → MCP server → pc_resources → Windows 本机
```

- [ ] 启动 MCP server + tool_router
- [ ] 用自然语言触发 M-IR 生成，验证文件写入
- [ ] 验证进程执行（起一个命令，读退出码）
- [ ] 验证网络连接（TCP 发包）

**启动**：
```powershell
# 终端 1：MCP 服务器
cd python\mcp
python server.py

# 终端 2：路由器
cd python\tool_router
uvicorn router:app --host 127.0.0.1 --port 8000
```

**冒烟测试**：
```powershell
cd tests
python smoke_e2e.py
```

---

## 术语映射（代码 vs 论文）

| 代码术语 | 论文术语 | 说明 |
|----------|----------|------|
| MVM | LVM | M 语言虚拟机 |
| M-Token | bytecode | 字节码格式 |
| M-IR | LIR | LLM 生成的中间语言 |
