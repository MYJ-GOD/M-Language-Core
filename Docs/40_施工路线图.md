# 施工路线图

> 更新时间：2026-07-24

## 当前状态

核心命题已验证：`自然语言 → LLM → M-IR → M-Token 字节码 → 执行`，端到端跑通（qwen3:8b 冒烟 3/3）。

**已通**：
- MVM 虚拟机（80+ 指令，完整安全约束）
- ESP8266 固件（串口执行字节码）
- MCP 工具层（25+ 工具）+ Skills 编排（4 个工作流）
- M-IR 编译器 + PC 端模拟器（15 个测试全绿）
- PC 资源设备化（文件槽，沙箱 + 路径穿越拦截）

**未通**：
- 真实服务链路（tool-router/WebUI 未接 M-IR）
- 控制计算机（仅文件写一个残缺切口）
- eBPF 策略平面（一行未写）
- 打包/安装/服务化（不存在）

---

## 阶段一：接通断点（真实服务链路）

**目标**：用户在 WebUI 说话 → LLM 生成 M-IR → 编译执行 → 返回结果

- [ ] 把 `execute_lir_mcp` / `execute_lir_action_mcp` 接进 tool-router
- [ ] 在 WebUI 端到端验证
- [ ] 补充 Skills 层测试（当前 0 覆盖）

**验证**：WebUI 说"帮我开继电器1"，LLM 生成 M-IR，编译执行，返回结果。

---

## 阶段二：PC 资源做实

**目标**：从"沙箱写预设内容"到"动态内容"

- [ ] 文件槽：支持动态内容（传感器读数写入文件）
- [ ] 进程槽：proc0 支持起/杀进程、读退出码
- [ ] 网络槽：net0 支持基础 TCP 收发

**验证**：M-IR 控制"读温度 → 写入文件 → 读文件内容"全链路。

---

## 阶段三：Linux + eBPF 策略平面

**目标**：在 Linux 上验证"LLM 意图 → 内核强制门控"

- [ ] 把 `require cap(...)` 映射成 seccomp-bpf 规则
- [ ] PoC：一条 M-IR → eBPF 策略注入 → 内核强制执行

**验证**：eBPF 策略阻止未授权的 syscall。

---

## 阶段四：服务化

**目标**：可安装、可开机自启

- [ ] 打包为 systemd 服务
- [ ] 一键安装脚本
- [ ] 配置文件标准化

**验证**：`apt install m-lang-core` 后服务自动启动。

---

## 术语映射（代码 vs 论文）

| 代码术语 | 论文术语 | 说明 |
|----------|----------|------|
| MVM | LVM | M 语言虚拟机 |
| M-Token | bytecode | 字节码格式 |
| M-IR | LIR | LLM 生成的中间语言 |
